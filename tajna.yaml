default: dev
envs:
  dev:
    FUM_LISTNAME: employees
    FUM_BASEURL:
        secret: prod-fum-baseurl
    FUM_PUBLICURL:
        secret: prod-fum-publicurl
    FUM_TOKEN:
        secret: prod-fum-token

    FD_AUTH_TOKEN:
        secret: prod-fd-authtoken
    FD_ORGANISATION: "futurice"
               
    # contacts is drunk
    FUM_USER_LIST: employees
    FUM_BASE_URL:
        secret: prod-fum-baseurl
    FUM_AUTH_TOKEN:
        secret: prod-fum-token
    GH_ORGANISATION: "futurice"

    POSTGRES_URL: postgres://$USER@localhost:5432/$USER
    POSTGRES_PASS: ""

    PLANMILL_BASEURL:
        secret: prod-planmill-futu-basurl
    PLANMILL_ADMIN:
        secret: prod-planmill-futu-userid
    PLANMILL_SIGNATURE:
        secret: prod-planmill-futu-signature

    GH_AUTH_TOKEN:
      secret: github-auth-token
    GH_ORG: "futurice"
    GH_TEAM:
      secret: github-team


    PROXYAPP_USER: timo
    PROXYAPP_PASS: secret
    FUTUHOURS_BASEURL: http://localhost:8081

    REPORTS_GH_REPOSURL:
      secret: reports-gh-repos

    # spice stats       
    FD_FLOW: "open-source"

    # PORTS
    AVATARAPP_PORT:     "8801"
    CONTACTSAPI_PORT:   "8802"
    FUTUHOURSAPI_PORT:  "8803"
    PROXYAPP_PORT:      "8804"
    SPICESTATSAPP_PORT: "8805"
    THEMEAPP_PORT:      "8806"
    REPORTSAPP_PORT:    "8807"
    PLANMILLPROXY_PORT: "8808"

    # Legacy
    PORT: "8080"
