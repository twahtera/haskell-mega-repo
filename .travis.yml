# This file has been generated by travis-meta-yaml 0.2.0.0
# see https://github.com/phadej/travis-meta-yaml
sudo: false
language: c
before_install:
- sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
- git submodule update --init
- export PATH=/opt/alex/3.1.7/bin:$PATH
- export PATH=/opt/ghc/$GHCVER/bin:$PATH
- if [ -n "$CABALVER" ]; then export PATH=/opt/cabal/$CABALVER/bin:$PATH; fi
- export PATH=~/.local/bin:$PATH
- if [ ! -e ~/.local/bin/stack ]; then mkdir -p ~/.local/bin; travis_retry curl -L
  https://www.stackage.org/stack/linux-x86_64 | tar xz --wildcards --strip-components=1
  -C ~/.local/bin '*/stack'; fi
- echo "$(ghc --version) [$(ghc --print-project-git-commit-id 2> /dev/null || echo
  '?')]"
- stack +RTS -N2 -RTS --version
- ghc --version
install:
- JOB_START_TIME=$(date +%s)
- if [ "$STACK_SOLVER" = "YES" ]; then rm -f stack.yaml; stack update; stack init
  --solver; fi; stack --no-terminal --skip-ghc-check list-dependencies|sort > installplan.txt;
  cat installplan.txt; stack --no-terminal --skip-ghc-check setup; if diff -u $HOME/.stack-work-cache/installplan.txt
  installplan.txt; then echo "cabal build-cache HIT"; rm -rf .stack-work; cp -a $HOME/.stack-work-cache
  .stack-work; rm -f .stack-work/installplan.txt; else echo "cabal build-cache MISS";
  rm -rf $HOME/.stack-work-cache; JOB_CURR_TIME=$(date +%s); timeout $((JOB_START_TIME + 2400 - JOB_CURR_TIME)) stack --no-terminal --skip-ghc-check build --test
  --only-dependencies -j1 --ghc-options="-j2 +RTS -M2G -A128M -n2m -RTS"; fi; if [
  ! -d $HOME/.stack-work-cache ]; then echo "snapshotting package-db to build-cache";
  cp -a .stack-work $HOME/.stack-work-cache; cp -a installplan.txt $HOME/.stack-work-cache/installplan.txt;
  fi;
script:
- if [ "$PEDANTIC" = "YES" ]; then export STACKOPTS=--pedantic; fi
- JOB_CURR_TIME=$(date +%s)
- timeout $((JOB_START_TIME + 2400 - JOB_CURR_TIME)) stack --no-terminal --skip-ghc-check build --test $STACKOPTS -j1 --ghc-options="-j2
  +RTS -M2G -A128M -n2m -RTS"
- stack --no-terminal --skip-ghc-check sdist
matrix:
  fast_finish: true
  include:
  - env: GHCVER=7.10.3 STACK_YAML=stack-lts-6.yaml PEDANTIC=YES
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - libfftw3-dev
        - alex-3.1.7
        - ghc-7.10.3
        - libgmp-dev
    compiler: ! ': # ghc-7.10.3 stack-lts-6.yaml'
  - env: GHCVER=8.0.1 STACK_YAML=stack-ghc-8.0.yaml
    addons:
      apt:
        sources:
        - hvr-ghc
        packages:
        - libfftw3-dev
        - alex-3.1.7
        - ghc-8.0.1
        - libgmp-dev
    compiler: ! ': # ghc-8.0.1 stack-ghc-8.0.yaml'
branches:
  only:
  - master
cache:
  directories:
  - ~/.stack
  - ~/.local
  - ~/.stack-work-cache
  apt: true
git:
  submodules: false
